import 'package:flutter/material.dart';

class FlutterDataTable extends StatelessWidget {
  const FlutterDataTable({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(
        children: [],
      ),
    );
  }
}
